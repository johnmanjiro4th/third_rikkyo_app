<div class="buy_item_area">
  買った商品
  <% @buy_transactions.each do |transaction| %>
    <%= link_to "#{transaction.id}", transaction_show_path(transaction.id) %>
  <% end %>
</div>
<div class="sell_item_area">
  売った商品
  <% @sell_transactions.each do |transaction| %>
    <%= link_to "#{transaction.id}", transaction_show_path(transaction.id) %>
  <% end %>
</div>
